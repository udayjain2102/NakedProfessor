// Runtime configuration for the deployed static site.
// Edit this file on the host (or replace during deploy) without rebuilding.
window.__NP_RUNTIME_CONFIG__ = {
  ADSENSE_SLOT_TOP_BANNER: "",
};

